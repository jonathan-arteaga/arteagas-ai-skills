# Current contracts
Button is the current shared primitive. CompactButton intentionally uses 4px padding in dense table rows; keep this variant. LegacyButton is deprecated and names Button as its replacement. Destructive actions require a distinct role; the system does not currently define one. Equal visual values do not establish equal intent.
